# Debug Dockerfile with Vite dev server and HMR support.
FROM node:22-alpine

RUN corepack enable

WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY . .

# 5173: Vite dev server
EXPOSE 5173

CMD ["pnpm", "dev", "--host", "0.0.0.0"]
